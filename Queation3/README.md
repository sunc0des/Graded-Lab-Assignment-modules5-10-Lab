Question 3

This folder contains:
- validate_results.sh
- marks.txt
- Screenshots


- Explanations for each command:

Command:
nano marks.txt

Explanation:
This command creates the input file containing student marks data.

Command:
nano validate_results.sh

Explanation:
This command creates a shell script to analyze student results.

Command:
chmod +x validate_results.sh

Explanation:
This command gives execute permission to the script.

Command:
./validate_results.sh

Explanation:
This command runs the script and displays students who passed all subjects or failed exactly one subject along with counts.

